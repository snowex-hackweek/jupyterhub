This will create an IAM Role that AWS Account Users can "assume" to perform all things needed for JHUB infrastructure

We can also assume this role in CI workflows (GitHub Actions) to run commands that require authenthication
